# 성취수준 RC3 수정 및 독립 회귀 평가 — 2026-09-20

## 고정 후보

- 버전 `1.1.0-rc.3`, profile `1.2.0`
- 소스 `0b86fd13faba8ea647bcfcaa86de2a3d61a12bf5`
- 배포 `dpl_211AVkyqNFKGrxaA8ACG9bLX4ero`, READY
- [검증 MCP](https://edunet-ovdihod7q-raphysicst.vercel.app/api/mcp), [배포 상세](https://vercel.com/raphysicst/edunet-mcp/211AVkyqNFKGrxaA8ACG9bLX4ero)
- sourceDigest `3ae286794d83416fc2f5ecd314e57fec383edcbf8980e31edf77e848b5e6d803`
- 원격 manifestDigest `76fe48e149d781eef89e403aaf37eb743e68f5cb7ddee71e7327ec9bea7530db`

로컬의 제품 입력 경로는 clean이며, 원격의 전체 파일 해시와 sourceDigest·sourceCommit이 일치합니다. Git 메타데이터가 없는 원격의 `gitClean:null`은 확인 불가를 뜻합니다. `vercel.json`의 플랫폼 재직렬화는 모든 설정을 보존하는 키 정렬 JSON 해시로 검증합니다. [RC2 배포 동일성 진단](achievement-rc2-validation-2026-09-20.md)에 원인과 재현 증거가 있습니다.

기존 운영 환경을 사용하는 `--prod --skip-domain` 검증 배포입니다. 공개 서비스 도메인을 이 후보로 승격하지 않았습니다. 배포 보호를 유지하며 인증값은 고정 origin 요청에만 사용합니다.

## RC2에서 확인해 수정한 항목

| 문제 | RC3 변경과 한계 |
|---|---|
| 초등 교과별 요청이 학년군 묶음 제목을 제외함 | 해당 초등 교과는 학교급 목록으로 탐색. 실제 원문 교과를 메타데이터에서 만들어 넣지 않음 |
| 공통과목명과 게시판의 교과 묶음 제목이 다름 | `공통영어1`→`영어`, `한국사1`→`역사` 등 제목 탐색 조건을 보완. 과목명 검색의 검증된 총건수가 0이면 학교급·페이지·크기·시간 한도 안에서 한 번만 키워드 완화하고 경고 |
| 괄호/과목 번호가 있는 코드 미인식 | `[9사(지리)01-01]`, `[10공영1-01-01]` 같은 원문 형식을 보존. 잘못 닫힌 괄호 등은 거절 |
| 수학 A/B·C/D 공동 설명 셀이 복원되지 않음 | 라벨 열의 행 경계와 성취기준·설명 열의 실제 선을 각각 확인해 병합. 일부만 가로지르는 선은 불확실한 것으로 거절하며 빈 셀로 범위를 추측하지 않음 |
| 기준만 있는 추가 레코드가 검증된 성취수준에 섞임 | 수준 라벨과 설명이 함께 있는 항목만 성취수준 레코드로 반환. 기준 원문은 삭제하지 않고, 수준 미확정 시 raw fallback 제공 및 제외 경고 |
| 문서 전체 IPC 출력이 한도를 초과하고 다른 읽기에 circuit 전파 | 다른 필터 없는 코드 지정 요청은 서명된 작업 범위를 Worker에 전달하여 IPC 전에 선택. 선택한 완전 레코드가 모두 20,000자 이하면 쓰이지 않는 전체 raw 블록을 보내지 않음. 문서 출력 제한은 전역 인프라 장애로 집계하지 않음 |

10MiB 다운로드·Worker 출력·프로세스 메모리·실행 시간·공개 응답 크기 제한은 늘리지 않았습니다. 코드 없는 큰 문서나 구조화 실패 문서에는 출력 제한이 여전히 적용됩니다. 실제 Worker crash, timeout, 잘못된 IPC의 회로 차단 동작은 유지합니다.

## 개발 검사와 원격 연결 검사

| 검사 | 결과 |
|---|---|
| 전체 타입/제품 검사 | PASS, 제품 229/229 |
| 최종 계약 검사 | 292/292 PASS — 제품 229개 + 기존 평가기 검사 63개 |
| 기존 합성 golden | 8/8 PASS |
| 원격 소스 동일성 | 로컬/원격 전체 파일 해시 일치 |
| 실제 과학 PDF 연결 검사 | 목표 자료·첨부 도달, `[9과05-01]` A–E 5개 |
| 실제 고등 정보 HWP 코드 지정 읽기 | `[12정01-01]` 수준 5개·1페이지 반환, 뒤 일반 검색 2건 반환 |
| 실제 참조 만료와 일반 검색 복구 | 만료 전 첨부 2개, 15분 만료 후 `INVALID_REFERENCE`, 같은 MCP 연결에서 일반 검색 2개 반환 |
| 개발 원문 대조 | 수학/사회 A–E 5개 및 고등 공통영어 코드 형식 확인. 독립 RC 건수에 합산하지 않음 |

마지막 목록 응답 건수 일관성 보강도 최종 계약 검사에 포함됩니다. 이전 버전의 통과 수를 재사용하지 않았습니다. 산출물은 `evals/results/remediation-development/`의 `contract-rc3.json`, `golden-rc3.json`, `rc3-smoke.log`, `rc3-info-smoke.json`, 로컬/원격 manifest 및 배포 증거에 보존합니다.

만료 검사는 실제 발급 `08:37:29.721Z`, 만료 `08:52:29.721Z`인 같은 참조를 사용했습니다. 만료 전 `08:37:30.310Z`와 만료 후 `08:52:57.209Z`의 참조 SHA256·소스 digest가 일치합니다. 안전한 증거는 `rc3-pre-expiry.json`과 `rc3-expiry-recovery.json`이며 참조 값은 공개 기록에 포함하지 않습니다. 이 보조 검사는 독립 90건, Worker crash/timeout 또는 cold start 검증에 합산하지 않습니다. 로컬 제품 검사에는 실제 child process의 crash·stall·잘못된 IPC 도중과 이후에 일반 MCP 검색이 유지되는 검사가 포함되지만, 운영과 유사한 원격 환경의 장애 주입을 대신하지 않습니다.

`08:50:37Z` 공개 도메인 직접 조회에서도 기존 배포 `dpl_B75SY33uabZUFmRSUN7QhkddXCDK`가 READY였습니다. 증거는 `rc3-production-alias-evidence.json`입니다.

## 독립 평가와 비교 범위

별도 작업 `성취수준 RC 독립 평가자 구현 및 평가`에 `work/rc3-handoff/rc-lock.json`을 전달했습니다. 제품 소스·개발 파서 출력을 읽지 않는 평가자가 RC2의 최종 v3 수집 프로토콜과 동일한 원문 30종·90개 시나리오를 처음부터 재실행합니다. 코드 결과가 없는 원문 부분 수집을 성공으로 바꾸지 않고, 구조화 응답의 cursor는 끝까지 수집합니다.

corpus SHA256은 `4602a056b74e8b4c1581efb4b2859e8be0c5180d53a06356f84d4eef4a3600aa`로 유지합니다. 평가기 자체 검사는 49/49 통과했습니다. 새 결과는 `outputs/runs/rc3-20260920-v3/`에 별도로 보존합니다. RC2 실행·정답·점수를 덮어쓰거나 새 점수에 합산하지 않습니다.

RC2에서 이미 관측한 실패를 수정하고 같은 corpus로 확인하는 회귀 평가입니다. 새로 보지 않은 표본의 블라인드 성능으로 해석하지 않습니다. 초기 보고서 열람 및 수집 범위 변경의 독립성 한계는 RC2 보고서와 함께 유지합니다. 실제 모델 최종 답변, 사람의 표 관계 최종 검토, 통제된 Worker 장애주입 및 누락된 최종 출시 Gate는 자동 검사로 대신 채우지 않습니다.

## 독립 v3 실행 결과와 수집기 감사

실행은 `08:37:05Z`부터 `08:57:24Z`까지 90건을 처리했습니다. 원문 부분 수집 3건·예외 0건이며, manifest 전후와 RPC 263/263개의 배포 digest가 일치했습니다.

| 관측 항목 | RC2 v3 | RC3 v3 |
|---|---:|---:|
| 첫 검색 응답에서 목표 발견 | 51/90 | 85/90 |
| 첨부 해결 | 51/90 | 85/90 |
| 다운로드 | 44/90 | 74/90 |
| 구조화 반환 | 39/90 | 71/90 |
| 고정 레코드 정밀도 | 157/190 (82.63%) | 264/291 (90.72%) |
| 관측 인용 원문 존재 | 2,248/2,248 | 3,588/3,588 |
| 부분 원문 수집 | 5 | 3 |

고정 채점의 RC3 불일치 27개는 초등 문서의 `초등학교 1~2학년군` 등 실제 원문에 있는 결합 학년 표기가 정답 허용값에 빠진 사례입니다. 독립 평가자의 사후 원문 감사에서 확인했으며, 점수를 소급 수정하거나 확정 제품 오류 27건으로 보고하지 않습니다. 반환된 291개 레코드의 코드·성취기준·수준·설명 1,164개 핵심 필드는 모두 일치했습니다. 이 진단과 고정 자동 점수는 별도입니다.

한국사 HWP 3건은 내려받은 원문 hash가 일치하지만 구조화 반환이 없어 raw 부분 수집으로 끝났습니다. 개발 진단에서 `성취기준별 성취수준 진술` 머리글을 인식하지 못한 누락을 확인하여 RC4에서 수정합니다. RC3의 `WORKER_OUTPUT_TOO_LARGE` 및 그에 따른 circuit 전파는 재현되지 않았습니다.

또한 RC3의 나머지 발견 5건은 모두 `hasNext:true,nextPage:2`였으나 **평가기 v3가 첫 검색 페이지만 읽은 결함**이 확인되었습니다. RC2의 발견 실패 39건 중 10건에도 같은 한계가 있습니다. 따라서 51/90→85/90은 첫 응답 범위의 비교이고, 5건을 끝까지 찾아도 없는 문서로 해석하지 않습니다. 당시 원본·점수·complete 플래그를 보존하되 완전한 검색 평가로 쓰지 않습니다. RC4는 독립 평가자가 후속 페이지 처리·거짓 종료·반복 페이지 검사를 보완한 새 v4 프로토콜을 고정하고 전체 90건을 다시 실행합니다.

최종 원본은 별도 평가 작업의 `outputs/runs/rc3-20260920-v3/final-report.md`, `score.json`, `analysis.json`, `post-score-source-audit.json`, `final-artifact-hashes.json`에 있습니다. 페이지 감사는 `outputs/discovery-pagination-audit.json`입니다. 수집기 수정으로 프로토콜이 바뀐 후의 수치는 제품 변경만의 개선 효과로 해석하지 않습니다. 이 문서는 출시 승인서가 아닙니다.
