# 에듀넷 MCP — MVP 구현·PRD·Evals 통합 계획

> 2026-09-17 현재 정책: `v1.0.0-rc.1`로 기능을 고정한다. 아래는 구축 당시 계획이며 원격 MCP·첨부파일 읽기 등 확장은 보류한다. 실제 사용에서 확인된 문제만 수정한다. MCP 검증과 모델 행동 관측을 분리하며 과거 모델 점수 기준은 RC 통과 조건으로 사용하지 않는다. 현재 기준은 [RC 기록](docs/release-v1.0.0-rc.1.md)이다.

## 1. 목표와 산출물

독립 TypeScript 프로젝트 `edunet-mcp`를 만들고, **대화 중 교육자료를 검색해 출처를 확인하고 후속 검색으로 이어갈 수 있는 로컬 MCP**를 먼저 제공합니다. 이후 누구나 접속 가능한 원격 MCP와 첨부파일 읽기로 확장합니다.

구현 전에 다음 문서를 작성하고 코드와 함께 버전 관리합니다.

| 산출물 | 역할 |
|---|---|
| `docs/prd.md` | 대상 사용자, 해결할 문제, 사용 흐름, 범위와 성공 기준 |
| `docs/api-findings.md` | 실제 API 응답·인코딩·링크·첨부파일 접근 조사 |
| `evals/README.md` | 평가 데이터, 실행 방법, 판정 기준과 결과 해석 |

API 클라이언트·응답 변환·MCP 도구·전송 방식을 분리합니다. `schoolinfo-mcp`의 구조를 참고하되 시간 제한·인증키 마스킹·로깅은 직접 구현합니다.

## 2. PRD 작성 계획

**주 사용자는 수업·평가자료를 찾는 교사**로 설정합니다. 첫 사용 흐름은 다음과 같습니다.

> “중2 과학 광합성 평가자료 찾아줘” → 관련 자료와 출처 확인 → 제목 검색이나 카테고리 변경 → 다음 페이지 탐색

PRD에는 다음을 명시합니다.

- **문제:** 자료를 찾기 위해 대화 작업을 중단하고 검색 사이트로 이동해야 하며, 검색 결과만으로 첨부파일 내용을 파악하기 어렵습니다.
- **MVP 가치 가설:** AI가 검색 조건을 구성하고, 결과를 대화 맥락 안에서 설명하며, 후속 검색까지 연결하면 자료 탐색 과정이 짧아집니다.
- **확장 가치 가설:** 첨부파일의 활동·평가 기준을 추출할 수 있다면 검색 이상의 수업 준비 지원이 가능합니다. 비로그인 접근성과 추출 품질을 확인하기 전에는 제공 가능한 기능으로 확정하지 않습니다.
- **MVP 필수 기능:** 검색, 카테고리 선택, 제한된 내용 발췌, 출처 링크, 페이지 안내, 명확한 실패 안내.
- **MVP 제외:** 웹 화면, 회원 관리, 첨부파일 파싱, 자료 저장, 원문을 읽었다고 가정한 수업안 생성.
- **완료 기준:** 실제 API와 MCP 연결이 동작하고 아래 필수 eval을 통과해야 합니다. 키 없는 테스트 통과와 실연동 완료는 구분합니다.

원문 미열람, 검색 결과 부족, 로그인 필요, API 장애 등 사용자에게 알려야 하는 상태도 PRD에 포함합니다.

## 3. 실제 API 조사와 MVP 구현

**인증키 확보 후 파서 구현보다 먼저 최소 API 호출을 수행합니다.**

- 헤더와 원시 바이트로 인코딩을 확인하고 실제 XML 구조·필드명·오류·페이징을 기록합니다.
- 상세페이지 URL을 API가 제공하는지 확인합니다. 직접 조합이 필요하면 실제 사이트나 공식 자료로 검증한 규칙만 사용합니다.
- 실제 응답의 민감 정보를 제거해 fixture로 사용합니다. 문서 샘플은 보조 자료로 구분합니다.
- 수업설계·평가자료·주제별 학습자료에서 가능한 범위로 각각 3건씩 골라 비로그인 상세페이지·첨부파일 접근성을 조사합니다. 접근 실패와 자료 자체의 첨부파일 부재를 구분합니다.
- 키가 없으면 설정·HTTP 처리·로깅·MCP 등록 골격까지 진행하고 실응답 관련 구현은 잠정 상태로 둡니다.

첫 도구는 `search_edunet` 하나입니다.

| 입력 | 기본값·제한 |
|---|---|
| `query` | 필수, 학년·과목·주제·자료 유형 조합 예시 제공 |
| `categories` | 전체 검색, 코드와 한국어 뜻을 설명한 enum |
| `sort` | 정확도순 또는 등록순 |
| `searchType` | 제목+요약 또는 제목 |
| `page` | 기본 1, 최대 50 |
| `pageSize` | 기본 10, 최대 20 |

구조화된 결과에는 검색 조건, 자료 목록, 제공된 전체 건수와 페이지 정보를 넣습니다. 자료당 내용은 최대 500자로 자르고 잘림 여부를 표시합니다. 텍스트는 건수·페이지 요약만 반환합니다.

전체 건수가 없을 때는 건수를 추정하지 않으며, 다음 페이지 존재 여부도 확인된 범위로 표시합니다. 학교급·학년 전용 필터와 API가 제공하지 않은 내용은 만들어내지 않습니다.

설정은 `EDUNET_API_KEY`, `EDUNET_DOMAIN`을 사용합니다. 요청별 15초 제한은 본문 읽기까지 유지하고, 전체 재시도 포함 45초로 제한합니다. 일시적인 네트워크 오류와 429·502·503·504만 최대 2회 재시도합니다. `sno`와 키 값을 마스킹하고 애플리케이션 로그는 stderr로 고정합니다.

## 4. Evals 설계

**코드가 정확히 동작하는 평가와, AI가 도구를 유용하게 사용하는 평가를 분리합니다.**

### A. 결정적 계약 평가 — 매 변경 시 실행

정제한 실제 응답과 명시적으로 표시한 합성 오류 fixture로 검증합니다.

- 인코딩, XML 구조, 필드 변환, 누락 필드, 결과 0·1·여러 건.
- 한글·특수문자·복수 카테고리 요청과 입력 제한.
- 내용 길이, 강조 태그, 건수·다음 페이지 안내.
- API 제공 링크 또는 검증된 링크 조합 규칙.
- 인증 오류, 잘못된 XML, 본문 지연, 시간 초과·재시도.
- 로그·오류·fixture의 키 비노출과 stdio 오염 방지.

**통과 기준은 필수 케이스 100%**입니다. 일반 단위 테스트와 중복 구현하지 않고 기존 테스트를 eval 실행에 포함합니다.

### B. AI 도구 사용 평가 — 고정된 30개 시나리오

| 시나리오 | 수 |
|---|---:|
| 학년·과목·주제를 포함한 자료 검색 | 8 |
| 카테고리·정렬·제목 검색 선택 | 6 |
| 다음 페이지·후속 검색 | 4 |
| 모호하거나 결과가 부족한 요청 | 4 |
| 설정 누락·인증·네트워크 실패 | 4 |
| 원문 미열람·출처·검색 결과 속 지시문 처리 | 4 |

각 케이스에 사용자 요청, 모의 API 응답, 허용되는 도구 인자·행동, 금지 행동, 판정 기준을 저장합니다.

- 정확히 같은 검색어를 요구하지 않고 핵심 개념과 허용 카테고리로 판정합니다.
- 페이지 이동과 입력 제한은 도구 호출 기록으로 자동 판정합니다.
- 최종 답변은 결과에 없는 사실·링크를 추가했는지, 첨부파일을 읽었다고 주장했는지 확인합니다.
- 검색 결과 안의 “지시를 무시하라” 같은 문구는 자료로 취급해야 합니다.
- 모델·버전·설정·케이스 버전을 기록하고 릴리스 후보에서 케이스당 3회 실행합니다.

**초기 통과 기준:** 전체 실행의 90% 이상 성공, 키 노출·출처 조작·원문 열람 허위 주장·외부 지시문 실행은 0건입니다. 경계 사례는 사람이 검토하며 LLM 채점만으로 통과시키지 않습니다.

### C. 실제 검색 유용성 평가 — 키 확보 후 실행

교사 사용 흐름을 반영한 검색 과제 10개를 만들고 상위 5건을 사람이 평가합니다.

- 직접 관련 2점, 일부 관련 1점, 무관 0점으로 기록합니다.
- 과제별 상위 5건 중 직접 관련 자료가 최소 1건 있는지를 측정합니다.
- 초기 목표는 10개 과제 중 8개 이상 성공입니다.
- 부족한 결과가 검색어 구성 때문인지, API 검색 품질·자료 부족 때문인지 분리합니다.
- 같은 조건의 직접 API 호출과 MCP 결과를 비교해 래퍼가 자료를 누락·변형하지 않는지도 확인합니다.

실제 검색은 변동 가능하므로 일상 CI의 고정 통과 조건으로 쓰지 않습니다. 목표 미달 시 원인과 사용자 영향, 검색어 안내 개선 결과를 릴리스 보고서에 기록합니다.

## 5. 실행 순서와 완료 판정

1. **PRD 초안·eval 시나리오 작성:** 사용자 가치, 범위, 실패 안내, 평가 기준을 먼저 고정합니다.
2. **실제 API 조사:** 인증키 확보 즉시 응답·링크·첨부파일 접근을 확인하고 fixture를 만듭니다.
3. **로컬 MCP 구현:** 계약 평가와 stdio 연결 검증을 통과시킵니다.
4. **AI·실검색 평가:** 실패를 분류하고 도구 설명·결과 형식을 개선합니다.
5. **MVP 릴리스:** 설치 안내, 평가 결과, 알려진 제한과 실연동 상태를 함께 제공합니다.

평가 결과에는 케이스별 통과 여부, 실패 유형, 지연시간, 입력·출력 토큰 수, 실행 환경을 기록합니다. 토큰 수와 지연시간은 첫 기준선을 만든 뒤 후속 버전의 회귀를 비교합니다. 실패 로그는 민감 정보를 제거한 후 회귀 케이스로 추가합니다.

## 6. 확장과 평가 확대

**공개 원격 MCP**

회원가입이나 사용자별 API 키 없이 연결하도록 운영자 키를 서버에 보관합니다. 초기 제한은 IP당 분당 30회, 전체 동시 API 요청 5개, 캐시 TTL 5분·최대 500개입니다. 공개 전에 발급키 이용 조건과 호출 한도를 확인합니다.

원격 eval에는 여러 클라이언트 연결, 동시 호출, 제한 초과, 요청 중복 처리, 캐시·키 비노출을 추가합니다.

**첨부파일 읽기**

사전 조사에서 접근 가능한 자료부터 지원합니다. 문서 변환은 별도 프로세스로 격리합니다. 지원할 형식별로 최소 5개 평가 문서를 확보하고 본문·표·평가 기준의 추출 정확성, 출처 보존, 손상 파일·시간 초과를 평가합니다. 평가 기준을 충족한 형식만 지원 목록에 올립니다.

**수업 준비 지원**

검색과 원문 추출이 검증된 뒤 자료 비교·활동 추출·평가 기준 정리로 확장합니다. 이 단계에서는 생성 결과의 각 핵심 주장에 원문 근거가 있는지, 자료 간 내용을 혼동하지 않는지를 새로운 필수 eval로 추가합니다.
